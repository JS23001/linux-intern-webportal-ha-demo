# Local administrator credentials. Never commit private SSH keys.
secrets/
github info.txt

# Downloaded installation media and portable utilities are reproducible, but
# intentionally excluded from version control.
installer/
tools/
